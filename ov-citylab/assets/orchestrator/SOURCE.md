# Orchestrator policies

- `policy_v0.json` — centroid imitation policy (retrain with `tools/train_orchestrator.py`).

```bash
python ov-citylab/tools/train_orchestrator.py --logs /tmp/citylab_orch_logs/*.jsonl
```
